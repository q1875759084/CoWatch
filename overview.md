# CoWatch Recorder — 限速模型重构 + 分辨率回退

## 改动摘要

### 1. throttle.ts — 固定步长限速模型（替换 AIMD）

**旧模型（AIMD）：**
- 8Mbps 起步，1-20Mbps 区间
- +20% / -40% 比例调整，向上探测带宽
- 问题：探测带宽 = 先制造拥塞 → 周期性游戏卡顿

**新模型（固定步长 + 硬顶封死）：**
- 硬底 3Mbps（30% 上行），初始基准 5Mbps（50%），硬顶 7Mbps（70%）
- 上调：连续 3 次稳定上传 → +0.5 Mbps（保守步长），硬顶截断
- 下调：单次 actualBps < base × 85% → 瞬时降到实际能力
- 上传失败 → 减半，不低于硬底
- 从 3 恢复到 7 约需 4 分钟（8 轮 × 3 次确认 × 10s 片长）
- 上调只在积压期间发生，不限速期间不参与调整

### 2. 录制层分辨率 810p → 720p

`recording/index.ts`: maxWidth 1440 → 1280（硬件）/ 1280 → 854（软件）

720p@60fps CQ28 预估码率：MOBA ~5-6 Mbps，FPS ~9.5-14 Mbps

### 3. upload/index.ts — 注释同步更新

限速策略注释从 AIMD 改为固定步长，函数签名不变无需改调用。

### 4. 参考文档同步更新

`docs/recorder-encoding-reference.md` — Phase 4b/4c 章节：分辨率、码率、文件大小、上传限速描述、转码耗时、资源占用全部更新。

## 关键设计决策

| 决策 | 理由 |
|------|------|
| 硬顶 7Mbps（70% 上行） | 游戏永远有 30% = 3Mbps 头room，永不会因上传占满上行而卡顿 |
| 上调用固定步长 0.5Mbps | 避免 history buffer 陷阱（限速期间测量不反映真实能力），收敛确定 |
| 下调瞬时、上调缓慢 | 网络变差时立即保护游戏，变好时保守恢复（4 分钟） |
| 分辨率回 720p | 降低视频产出码率，减少积压压力，给限速模型观察空间 |

## 待后续优化

- 720p 是临时的，等限速模型稳定后可以考虑 810p 或更高
- 可做用户可配置的限速上限（如高级用户手动拉到 10Mbps）
