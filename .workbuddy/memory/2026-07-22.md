# 2026-07-22 集成：window_capture 改动核实 + 设计文档纠偏

## window_capture(exe) 侧改动核实（另一 AI 已完成，主理人独立读源码坐实）
读 `C:/Users/绝绝子/Desktop/Co/window_capture/src/{config.cpp,capture_session.cpp}` + `README.md`：
- **CLI 契约已稳定**（与 design docs 一致）：
  - `--hwnd <十进制>` 主契约：`cfg.hwnd = static_cast<uint64_t>(std::stoull(...))`（64 位，无截断）✅
  - `--window "title:class:exe"` + `--window-priority class|title|exe` 备选 ✅
  - `--pid` 降级兜底（uint32_t）✅
  - 裁决 hwnd>window>pid，皆无→退出码 1 ✅
  - `--mux-target file|null` 必需；`--out` → `resolve_output_path`（OBS 模型：.m3u8 完整路径/空→recordings/目录）✅
- **HWND→OBS 对齐**：`hwnd_to_window_string` 用 `GetWindowText/GetClassName/GetWindowThreadProcessId→exe` 拼 `title:class:exe`；`init()` 中 `obs_data_set_string(ss,"window",...)` + `obs_data_set_int(ss,"priority",0)`（class 默认），`method=2(WGC)` ✅ 完全对齐 OBS，无 PID/HWND 存储。
- **`--pid` 兜底已升级**：旧版「首个可见窗口」→ 现 `largest_visible_window_of_pid`（取该 PID **最大可见窗口**，比首个更稳；仍 lossy 非主路径）。
- **切片命名**：`ffmpeg HLS` 默认派生 → `--out=session_abc.m3u8` 时切片为 `session_abc0.ts/session_abc1.ts…`（**非旧 `segNNN_opt.ts`**）。
- **READY JSON** 含 `out`（m3u8 绝对路径）✅；优雅停止 `CTRL_C_EVENT`（capture_session::stop 轮询 `obs_output_active` 等尾段写 #EXT-X-ENDLIST）✅。

## 设计文档纠偏（2026-07-22 深夜，因另一 AI handoff 有误导风险）
`window_capture-integration.md` + `window_capture-window-select-impl.md` 三处与已验证实现/另一 AI handoff 打架，已修正：
1. **`--pid` 兜底实为「最大可见窗口」**（非文档原写的「首个可见窗口」）：两文档 CURRENT 态描述 + 函数名 `resolve_pid_to_window`→`largest_visible_window_of_pid` 全部改正（历史语境 line 4/50/62/86/360 保留准确）。
2. **HWND 64 位传参**：impl.md §3.9 伪码原 `parseInt(sourceId.split(':')[1],10)` → 改为直接取十进制串 `hwndStr` 原样下传（勿 `|0`/勿需 BigInt，config 端 stoull 收 uint64）；`CaptureProfile.hwnd` 类型改 `number|string`。
3. **chokidar「不用变」误导**：另一 AI handoff 称「chokidar 监听逻辑不用变」仅指**目录** `dirname(READY.out)` 不变；**文件名匹配规则 `_opt.ts`→`.ts` 必须改**（否则旧规则匹配不到 `session_abcN.ts`，切片全漏传）。两文档已加 ⚠️ 警示。
- 其余（上传层实时切片零改动、playlist 后端重建、screen 分支不动）维持上轮结论。

## 新窗口执行指引（CoWatch 侧，用户将拿文档去执行）
- **必读**：`electron/docs/window_capture-window-select-impl.md`（逐文件伪改动蓝图：profiles.ts / recording/index.ts / recorder/index.ts / sentinel-client.ts / window_sentinel.py / preload.ts）+ `electron/docs/window_capture-integration.md`（CLI 契约/输出路径/信号/上传边界）。
- **背景参考**：`CoWatch/.workbuddy/memory/2026-07-21.md`（5 决策）+ 本文件。
- **可选**：`Co/window_capture/README.md`（exe 侧契约，确认 spawn 命令与 READY JSON 形状；已与设计文档一致）。
- **铁律提醒**：CoWatch 侧只改窗口分支 + 上传 chokidar 文件名规则；screen 分支/上传层/throttle 零改动；停止用 CTRL_C_EVENT。

## 新窗口独立核实（2026-07-22 续 — 执行前核实，交付总监齐活林窗口）
- 已通读 impl.md / integration.md / 2026-07-21.md / 2026-07-22.md / window_capture/README.md，并独立读 CoWatch 现网源码核实。
- 文档对现网状态的行号/结构断言**基本属实**：spawnMuxer:248 / gracefulQuitWindow:287 / READY→spawnMuxer:234 / startWindowUploadWatcher:623 + `_opt.ts`匹配:634 / makeDefaultProfiles:326 / startSentinel(title):306 / sentinel title argv:513 / win_event_proc 用 target_hwnd:411+ / preload recorder:start:33。上传层零改动断言成立（`_opt.ts` 仅 recorder/index.ts:634 出现；upload/index.ts enqueueMissingFiles 已按 `.ts` 扫；throttle 纯文件流）。
- **ERRATA（执行前必须修正文档误导）**：
  1. 【关键】`recording/index.ts::getCaptureExePath`（:153-163）实际返回 `bin/window_capture.exe`，**并非** impl.md §3.7 / integration §6.5 声称的 `bin/window_capture/window_capture.exe` 子目录。exe 按 §1 搬到子目录 + 删旧 exe 后，此处不改成子目录 → 录制找不到 exe → onCrash。须改为 `window_capture/window_capture.exe`。
  2. 【次要】删 spawnMuxer 后，`profiles.ts::buildMuxArgs`（:108，仅 spawnMuxer:256 用）成死代码，tsc noUnusedLocals 可能报错；建议连 import（recording/index.ts:29）一起删。
  3. 【设计可选】impl.md §3.8 的 READY→onCaptureReady→建 chokidar 重构**非必须**：协调层已在 :350 对 tmpDir 起 watcher（早于 READY），因 `--out` 落 tmpDir，仅把 :634 匹配 `_opt.ts`→`.ts` 即功能完整且更最小改动。onCaptureReady 管线为可选增强。
  4. 【次要】spawn stdio 现 5 pipe（fd3/fd4 给 mux），删 mux 后未用但无害，可简化为 `['pipe','pipe','pipe']`（可选）。
- 结论：impl.md 已足够作为任务文档，**无需另起 task.md**；但执行工程师必须带上述 ERRATA（尤其 #1），以免被文档「已指向子目录/无需再改」误导。

## 工作区清理（2026-07-22 末，用户拍板「全删」）
判定并清理遗留工具链（用户逐项确认后执行，均为 gitignored/reproducible 的本地 scratch + 旧方案）：
- **删除**：`nul`（0字节误建）、`c：temptsc_out.txt`（全角冒号乱码 tsc 泄漏）、`123.bat`（capture-src 旧构建脚本，引用 tem/）、`electron/bin/capture-src/`（决策5 旧手搓方案）、`tem/`（257M，capture-src 构建依赖 scratch：NVENC SDK/ffmpeg 源码/cppwinrt）、瞬时日志 `tsc_out.txt`/`debug.log`/`cowatch_boot.log`/`out.txt`。
- **保留**：所有 modified recorder TS、`electron/docs/`（新集成文档）、`profiles.ts`/`types.ts`/`sentinel-client.ts`、11 个 memory、`__qa__/`、QA/架构评审报告、`electron/bin/` 下新件（window_capture.exe/window_sentinel.exe/ffmpeg dll/build-sentinel）。
- **归档建议（未执行）**：`docs/axy-*(window-capture-design/class-diagram/sequence-diagram)` 旧方案2a 设计，被 memory/历史报告引用，建议移 `docs/archive/`，勿删。
- **核验**：git status 已无 tem/123.bat/nul/c*temp；electron/bin 仅剩新方案件；`tem` 目录消失，`electron/bin` 292M（含新部署件）。
- 注：`tem/` 仅被 `123.bat` 引用且只为 capture-src 服务；新 window_capture.exe 在独立仓库 `Co/window_capture` 编译，不依赖 CoWatch/tem。

## 二次清理：temp/ 终端日志 + docs 孤儿 mermaid（2026-07-22 14:10）
用户拍板「精准清理」：
- **删 temp/ 终端日志(12)**：`7.10-终端1-6.md`、`7.15终端1-2.txt`、`终端7-10.md` —— 纯终端会话转储，已被 `.workbuddy/memory/2026-07-*.md` 提炼覆盖。
- **删 temp/ scratch .txt**：`wc_*.txt`(19)、`dep_*.txt`(3)、`capture1/2.txt`、`7-20.txt`，及草稿 `sequence-diagram-bug-ddagrab-crop.mermaid`。
- **迁 temp/ 2 个 .py → `electron/bin/build-sentinel/`**：`test_window_sentinel_debounce.py`(sentinel 防抖回归测试)、`debug1.py`（原稿从 temp 删，仅留 build-sentinel 一份）。
- **删 docs/ 孤儿 axy mermaid**：`axy-class-diagram.mermaid`、`axy-sequence-diagram.mermaid`（无任何 .md 引用）。
- **保留**：`docs/class-diagram.mermaid` + `docs/sequence-diagram.mermaid`（被 system_design.md / ddagrab-crop-incremental-design.md / axy-window-capture-design.md 引用，当前架构图）；temp/ 剩余 25 个 AI 设计笔记 + `ai.md`(tracked) 暂留。
- 核验：`docs/*.mermaid` 现仅剩 class/sequence 两个；build-sentinel 现含 window_sentinel.py + 2 个测试脚本；temp 余 25 文件全为笔记。
